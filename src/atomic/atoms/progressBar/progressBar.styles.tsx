import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 8,
    backgroundColor: '#EBEBEB',
    borderRadius: 4,
  },
  progress: {
    height: 8,
    backgroundColor: '#7A9A02',
    borderRadius: 4,
  },
});

export default styles;
