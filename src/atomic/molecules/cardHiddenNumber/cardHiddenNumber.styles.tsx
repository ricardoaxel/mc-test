import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    gap: 2,
    alignItems: 'center',
    padding: 5,
  },
  dots: {flexDirection: 'row', gap: -11},
});

export default styles;
